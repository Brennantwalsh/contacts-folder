<% @contacts.each do |contact| %>
  <h2><%= link_to contact.first_name, "/contacts/#{contact.id}" %></h2>
  <h3><%= contact.last_name %></h3>
  <h3><%= contact.email %></h3>
  <h3><%= contact.phone_number %></h3>
  <br>
<% end %>

