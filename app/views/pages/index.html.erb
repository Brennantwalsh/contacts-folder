<% @contacts.each do |contact| %>
  <h2><%= link_to contact.full_name, "/contacts/#{contact.id}" %></h2>
  <h3><%= contact.bio %></h3>
  <h3><%= contact.email %></h3>
  <h3><%= contact.regional_phone_number %></h3>
  <br>
<% end %>

